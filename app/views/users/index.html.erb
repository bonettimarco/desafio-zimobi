<h1>Usuários</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <table class="table">
      <thead>
        <tr>
          <th>Nome</th>
          <th>email</th>
          <th>tipo</th>
          <th>Criar/ ver anuncio</th>
        </tr>
      </thead>

      <tbody>
        <% @users.each do |usuario| %>
        <tr>
          <td><%= usuario.name %></td>
          <td><%= usuario.email %></td>
          <td><%= tipo usuario %></td>
          <td><%= anuncio usuario %></td>
        </tr>
        <% end %>
      </tbody>
      <%= link_to "Criar mais clientes para teste", new_user_path %>
    </table>
  </div>
</div>
